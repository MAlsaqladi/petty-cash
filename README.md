# نظام إدارة العهد النقدية للاتحادات

تطبيق بمعمارية Backend حقيقية: المتصفح **لا يتصل بـ Supabase مباشرة إطلاقًا ولا يعرف عنه أي شيء** (لا رابط ولا مفتاح). كل طلب يمر عبر خادم Node.js/Express (`server.js`) مستضاف على Render، وهو الوحيد الذي يملك بيانات الاتصال بقاعدة البيانات (محفوظة في متغيرات بيئة Render السرّية فقط).

```
المتصفح  →  server.js (Render)  →  Supabase (Postgres)
  (لا يعرف شيئًا عن Supabase)      (يملك مفتاح service_role السرّي)
```

## محتويات المستودع

| الملف | الوصف |
|---|---|
| `index.html` | الواجهة فقط — تتحدث مع خادمنا عبر مسار واحد `/api/db` |
| `server.js` | الخادم: يستقبل الطلبات من الواجهة وينفّذها فعليًا على Supabase باستخدام مفتاح service_role السرّي |
| `package.json` | تبعيات الخادم (`express`, `compression`, `@supabase/supabase-js`) |
| `schema.sql` | مخطط قاعدة البيانات الكامل — يُشغَّل مرة واحدة على مشروع Supabase |
| `README.md` | هذا الملف |

## خطوات الإعداد

### 1) إنشاء مشروع Supabase وتشغيل المخطط
1. أنشئ مشروعًا على [supabase.com](https://supabase.com).
2. **SQL Editor → New query** → الصق محتوى `schema.sql` كاملاً → **Run**.
   - هذا الملف أيضًا ينشئ bucket تخزين باسم `attachments` (خاص، غير عام) لحفظ مرفقات المصاريف والتحويلات (صور/PDF).
   - **إذا كنت شغّلت نسخة أقدم من هذا الملف سابقًا:** يكفي تشغيل الجزء الأخير منه فقط (سطر `insert into storage.buckets ...`) لإضافة bucket المرفقات دون التأثير على بياناتك الحالية.

### 2) إضافة أول اتحاد
**Table Editor → federations → Insert row**:

| federation_id | federation_name_ar | federation_name_en |
|---|---|---|
| `fed_001` | الاتحاد السعودي لكرة القدم | Saudi Football Federation |

### 3) الحصول على مفاتيح الاتصال (على السيرفر فقط)
من **Project Settings → API** انسخ:
- **Project URL** (`https://xxxxx.supabase.co`)
- **مفتاح service_role السرّي** — في النظام الجديد اسمه غالبًا `SUPABASE_SECRET_KEY` (يبدأ بـ `sb_secret_...`)، وفي النظام القديم اسمه `service_role` (يبدأ بـ `eyJ...`).

⚠️ **هذا المفتاح لا يوضع أبدًا داخل `index.html` أو أي كود يُرسل للمتصفح** — فقط في متغيرات بيئة Render (الخطوة التالية).

### 4) الرفع على GitHub

```bash
git init
git add index.html server.js package.json schema.sql README.md
git commit -m "نظام إدارة العهد النقدية"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```

### 5) النشر على Render كـ Web Service

1. **New + → Web Service** → اربط المستودع.
2. الإعدادات:

| الحقل | القيمة |
|---|---|
| **Language** | Node |
| **Build Command** | `npm install` |
| **Start Command** | `node server.js` |

3. **هذه هي الخطوة المهمة** — في قسم **Environment Variables** أضف متغيرين:

| Name | Value |
|---|---|
| `SUPABASE_URL` | `https://xxxxx.supabase.co` (رابط مشروعك) |
| `SUPABASE_SERVICE_KEY` | مفتاح service_role السرّي (من الخطوة 3 أعلاه) |

4. **Create Web Service**. بعد انتهاء البناء يصبح التطبيق متاحًا على رابط مثل:
   `https://petty-cash.onrender.com`

### 6) أول تسجيل دخول
بما أنه لا يوجد مستخدمون بعد، ينشئ التطبيق تلقائيًا حساب **"رئيس اتحاد"** تجريبي لكل اتحاد، ويعرض اسم المستخدم وكلمة المرور على شاشة الدخول مباشرة. سجّل الدخول به، أنشئ بقية المستخدمين، وغيّر كلمة المرور فورًا من "ملفي الشخصي".

## لماذا هذا أفضل أمنيًا من الإصدار السابق؟

في الإصدار الذي كان يعتمد على مفتاح `anon` داخل `index.html`:
- أي شخص يفتح كود الصفحة (View Source) يرى رابط ومفتاح قاعدة البيانات ويقدر يصل لها مباشرة عبر REST API.

في هذا الإصدار:
- المتصفح لا يملك أي بيانات اتصال بقاعدة البيانات إطلاقًا — فقط رابط موقعك.
- قاعدة البيانات في Supabase **مقفلة بالكامل أمام أي وصول مباشر من الإنترنت** (RLS بلا سياسات = رفض تلقائي لأي طلب لا يحمل مفتاح service_role).
- الوصول الوحيد الممكن هو عبر خادمك على Render، الذي يمكنك مراقبته وتقييده لاحقًا (مثل: تحديد عدد الطلبات، أو إضافة تحقق إضافي على مستوى الجلسة).

## المرفقات (صور / PDF)

عند تسجيل مصروف فعلي أو تنفيذ تحويل، يمكن إرفاق صورة أو ملف PDF (حتى 8 ميجابايت). تُرفع الملفات إلى Supabase Storage عبر الخادم فقط (لا يوجد اتصال مباشر من المتصفح)، وتُعرض لاحقًا برابط مؤقت صالح لـ 10 دقائق عبر زر "عرض المرفق" في نافذة تفاصيل السجل.

## النسخ الاحتياطي

من القائمة الجانبية في التطبيق زر **"نسخة احتياطية (Excel)"** يُصدّر كل البيانات الحالية إلى ملف xlsx على جهازك.

## هيكل قاعدة البيانات (ملخص)

- `federations` — الاتحادات
- `users` — المستخدمون (رئيس اتحاد / مدير تنفيذي / محاسب / موظف / مراجع)
- `cost_centers` — مراكز التكلفة (7 رئيسية ثابتة لكل اتحاد + فرعية)
- `expense_types` — أنواع المصاريف (موحّدة لكل الاتحادات)
- `custodies` — طلبات/سجلات العهد النقدية
- `custody_budgets` — المصاريف التقديرية لكل عهدة
- `custody_transfers` — التحويلات إلى كل عهدة
- `custody_closures` — المصاريف الفعلية لكل عهدة

راجع `schema.sql` للتفاصيل الكاملة لكل عمود.
