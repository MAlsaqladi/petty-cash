-- =====================================================================
-- ترقية: تعديل مسار اعتماد قرار التكليف وصرف تذاكر السفر
-- شغّلها مرة واحدة على قاعدة بياناتك الحالية:
-- Supabase Dashboard → SQL Editor → New query → الصق هذا الملف كاملاً → Run
-- آمنة لإعادة التشغيل أكثر من مرة (idempotent) ولا تحذف أي بيانات.
-- =====================================================================

-- علم يوضح هل أدخل المحاسب بيانات التذكرة (سعرها ومرفقها) لهذا المشارك أم
-- لا بعد — يُستخدم للتحقق قبل إرسال الرحلة لاعتماد الصرف النهائي، ولإخفاء
-- سعر التذكرة/المرفق في الواجهة والطباعة قبل إدخالهما فعليًا.
alter table delegations add column if not exists ticket_submitted boolean not null default false;

-- ملاحظة: لا حاجة لأي تعديل على عمود trip_status نفسه — فهو نص حر (text) بلا
-- قيد enum، والحالة الجديدة "قيد اعتماد الصرف" تُستخدم مباشرة من الخادم دون
-- أي تغيير في بنية الجدول.
